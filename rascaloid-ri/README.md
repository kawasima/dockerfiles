# Rascaloid RI(Reference Implementation)

```
% docker pull kawasima/rascaloid-ri
% docker run -e "http_proxy=" \
    -e "RASCALOID_HOST=[rascaloid api host]" \
    -e "RASCALOID_PORT=[rascaloid api port]" \
    -it --rm kawasima/rascaloid-ri
```
